# Arsenal Media v10 Broken Page Fix

This build fixes the broken extensionless service and portfolio URLs by creating real folder-based pages with index.html files:

- /services/custom-app-development/
- /services/contractor-crm-software/
- /services/website-design/
- /services/seo-services/
- /portfolio/mfr-roofing/
- /portfolio/decorative-curbing-landscape/

It also redirects the no-slash and .html versions to the canonical folder URLs, adds fuller content to the service pages, improves the two broken case studies, updates sitemap.xml, updates llms.txt, and updates links to use clean internal URLs.
